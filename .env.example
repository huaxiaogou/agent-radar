# 正式公开信号必须经过外部 LLM 的中文编辑。auto 会优先兼容现有 OpenAI 配置；
# 只配置 DeepSeek Key 时自动使用 DeepSeek。rules 仅用于诊断和待回填存储，不能生成公开卡片。
# 可显式填写 auto、deepseek、openai 或 rules。
RADAR_AI_PROVIDER=auto

# DeepSeek（推荐用于本站的结构化摘要任务）。Key 只保存在服务器 .env.production。
DEEPSEEK_API_KEY=
RADAR_DEEPSEEK_MODEL=deepseek-v4-flash
RADAR_DEEPSEEK_BASE_URL=https://api.deepseek.com

# OpenAI（继续兼容已有部署）。
OPENAI_API_KEY=
RADAR_OPENAI_MODEL=gpt-5.6-terra

# 正式默认值通常无需修改。
# 只限制最终公开数量；所有通过正文补全与发现阈值的候选都会进入配置的 LLM。
RADAR_MAX_NEW_ITEMS=48
# 历史中文编辑回填并发；不限制回填总条数。
RADAR_BACKFILL_CONCURRENCY=4
# 概念知识历史回溯：每批文章数、批内 LLM 并发和单篇失败后的有界重试。
RADAR_CONCEPT_BACKFILL_BATCH_SIZE=20
RADAR_CONCEPT_BACKFILL_CONCURRENCY=2
RADAR_CONCEPT_ANALYSIS_CONCURRENCY=2
RADAR_CONCEPT_ANALYSIS_ATTEMPTS=2
# 每次正常采集的概念知识分析总上限；新文章绝对优先，超出部分保留为 pending。
RADAR_CONCEPT_INCREMENTAL_BATCH_SIZE=20
# 历史概念失败/未完成记录的子上限；只使用上述总预算中处理新文章后的剩余额度。
RADAR_CONCEPT_RETRY_BATCH_SIZE=4
# 完整知识对象通常需要比信号摘要更长的输出预算和超时。
RADAR_DEEPSEEK_CONCEPT_MAX_TOKENS=6000
RADAR_DEEPSEEK_CONCEPT_TIMEOUT_MS=120000
RADAR_OPENAI_CONCEPT_TIMEOUT_MS=120000
RADAR_OPENAI_CONCEPT_MAX_TOKENS=6000
# 可选：概念综合使用独立模型；留空时复用 RADAR_DEEPSEEK_MODEL / RADAR_OPENAI_MODEL。
RADAR_DEEPSEEK_CONCEPT_MODEL=
RADAR_OPENAI_CONCEPT_MODEL=
RADAR_RELEVANCE_THRESHOLD=5
# 宽进严出：先保留少量边界候选，正文补全和 AI 分析后再执行发布阈值。
RADAR_DISCOVERY_RELEVANCE_THRESHOLD=3
RADAR_MAX_ITEM_AGE_DAYS=120

# systemd 每小时唤醒；90 个来源再按各自 1h/2h/4h/8h/12h/24h cadence 过滤。
# 中国大陆等跨境链路建议先降低来源并发并适当放宽单次请求超时。
RADAR_SOURCE_CONCURRENCY=2
RADAR_FETCH_TIMEOUT_MS=30000

# 可选：提升 GitHub API 直连限额。程序只会把该凭据发送给 primary 的 api.github.com；
# 非 GitHub 主机、catalog fallback、relay 以及跨域重定向都不会携带此凭据。
GITHUB_TOKEN=

# /models 动态全景：systemd 每次醒来都会判断，默认每 24 小时刷新一次；手工 ingest 始终立即刷新。
# 有效模型数低于安全阈值时保留上次成功快照，避免上游结构漂移清空页面。
RADAR_MODEL_LANDSCAPE_CADENCE_HOURS=24
RADAR_MODEL_LANDSCAPE_MIN_MODELS=40

# 可选：仅在 primary 与所有 catalog fallback 都失败后使用自有 HTTPS relay。
# 模板必须是无 URL 凭据的 HTTPS URL，且恰好包含一个 {url}；程序会对目标 URL 编码。
# 不要填写不可控的公共代理。relay 应原样返回上游 body、HTTP 状态与 Content-Type。
RADAR_FETCH_RELAY_TEMPLATE=
