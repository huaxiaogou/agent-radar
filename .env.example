# 正式公开信号必须经过外部 LLM 的中文编辑。auto 会优先兼容现有 OpenAI 配置；
# 只配置 DeepSeek Key 时自动使用 DeepSeek。rules 仅用于诊断和待回填存储，不能生成公开卡片。
# 可显式填写 auto、deepseek、openai 或 rules。
RADAR_AI_PROVIDER=auto

# DeepSeek（推荐用于本站的结构化摘要任务）。Key 只保存在服务器 .env.production。
DEEPSEEK_API_KEY=
RADAR_DEEPSEEK_MODEL=deepseek-v4-flash
RADAR_DEEPSEEK_BASE_URL=https://api.deepseek.com

# OpenAI（继续兼容已有部署）。
OPENAI_API_KEY=
RADAR_OPENAI_MODEL=gpt-5.6-terra

# 正式默认值通常无需修改。
# 只限制最终公开数量；所有通过正文补全与发现阈值的候选都会进入配置的 LLM。
RADAR_MAX_NEW_ITEMS=48
# 历史中文编辑回填并发；不限制回填总条数。
RADAR_BACKFILL_CONCURRENCY=4
RADAR_RELEVANCE_THRESHOLD=5
# 宽进严出：先保留少量边界候选，正文补全和 AI 分析后再执行发布阈值。
RADAR_DISCOVERY_RELEVANCE_THRESHOLD=3
RADAR_MAX_ITEM_AGE_DAYS=120

# 中国大陆等跨境链路建议先降低来源并发并适当放宽单次请求超时。
RADAR_SOURCE_CONCURRENCY=2
RADAR_FETCH_TIMEOUT_MS=30000

# 可选：仅在 primary 与所有 catalog fallback 都失败后使用自有 HTTPS relay。
# 模板必须是无 URL 凭据的 HTTPS URL，且恰好包含一个 {url}；程序会对目标 URL 编码。
# 不要填写不可控的公共代理。relay 应原样返回上游 body、HTTP 状态与 Content-Type。
RADAR_FETCH_RELAY_TEMPLATE=
